# ── Core ──────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1

# ── Database ──────────────────────────────────────────────
sqlmodel==0.0.22
psycopg2-binary==2.9.10
alembic==1.14.1
pgvector==0.3.6

# ── Task Queue ────────────────────────────────────────────
celery[redis]==5.4.0
redis==5.2.1
flower==2.0.1

# ── Kafka ─────────────────────────────────────────────────
kafka-python-ng==2.2.3

# ── Logging ───────────────────────────────────────────────
python-json-logger==3.2.1
structlog>=24.4.0

# ── Tests ───────────────────────────────────────────────
pytest>=8.3.0
pytest-cov>=5.0.0
freezegun>=1.5.0
fakeredis>=2.26.0

# ── HTTP (for health checks, robots.txt, OpenRouter) ────
httpx==0.28.1
certifi>=2024.8.30
truststore>=0.9.2

# ── Ingestion layer ────────────────────────────────────────
scrapling                    # Used by benchmark scripts only; browser engine not used in production
markdownify>=0.14.1          # HTML to markdown conversion for clean text extraction
beautifulsoup4>=4.12.0       # HTML parsing for content and link extraction
lxml>=5.0.0                  # XML/HTML parsing (used by Scrapling and XMLConnector)
langdetect>=1.0.9            # ISO 639-1 language detection (local, no API key)
pdfplumber>=0.11.0           # PDF text + table extraction
feedparser>=6.0.11           # RSS 0.9–2.0, Atom, RDF feed parsing
boto3>=1.34.0                # AWS S3 artifact storage (optional — skipped if not configured)

# ── Benchmarking / optional backends ───────────────────────
# Crawl4AI is used by scripts/benchmark/ for apples-to-apples comparisons.
crawl4ai>=0.8.5

# ── Optional: Docling (layout-aware PDF extraction) ────────
# Docling gives richer table detection than pdfplumber but pulls in Torch
# + model weights (~1 GB). The PDFConnector transparently falls back to
# pdfplumber when Docling is not installed, so this is safe to omit.
#
# To enable:  pip install -r requirements-docling.txt
